git config global user.name '%%%%%'
git config global user.email '%%%%%%%%%'
git config --list %configuration information

git status
touch %%%%%.houzhui
git add '%%%%%%%'
git commit -m '%%%%%%%%%%'
vi '%%%%%%%%%%%'   % remend the file
ls % list the files in the reposition
pwd %print working drection 
clear
mkdir cangkuming  %make a direction 
cd cangkuming %  change direction
git init % initialize a reposition
ctrl o %save
:wq   %quit
cat filename %observe

rm -rf filename
git rm filename
git commit -m '%%%%%'   % remove a file

git push % update to github
git clone URL
git log % see branches
git restore . % remend
git reset --hard commitid %go back to any version
git branch ... %create branch
git branch -a %list branches
git branch -M main % create and go to main
git branch -b kwc % create and go to kwc
git checkout ...%to some branch
git checkout master(main) % to master
git merge ... %merge with...

git remote add chuangjieyouqian http://chuangjieyouqian:password@github.com/chuangjieyouqian/repositoryname.git
git push -u chuangjieyouqian branchname %firsttime pushing to make clear the push origin and branch
git remote -v % see remote network
git remote -v add upstream networkname 

git fetch upstream % get new upstream code
git merge upstream/main % update upstream code   and then push
